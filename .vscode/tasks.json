{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "yadice", //edit
            "command": "/usr/bin/gcc",
            //arguments passed to the compiler 
            "args": [
                //"-debug",
                //"-g",
                //"${file}",
                "yadice.c",
                "functions.c",
                //"-Wno-format",
                "-g",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                //"${fileBasenameNoExtension}",
               // "$(mysql_config", "--cflags)",
                //"$(mysql_config", "--libs)",
                //"$(mysql_config --libs)",
                //"-Wno-deprecated-declarations",
                //"-Wno-format-security",
                //"-lm",
                "`pkg-config", "--cflags", "gtk+-3.0`",
                "`pkg-config", "--libs", "gtk+-3.0`",
                "-export-dynamic"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}

//gcc -Wno-format -o part1-bin part1.c -Wno-deprecated-declarations -Wno-format-security -lm `pkg-config --cflags --libs gtk+-3.0` -export-dynamic
//gcc -Wno-format -o sqltest sqltest.c $(mysql_config --cflags) $(mysql_config --libs) -Wno-deprecated-declarations -Wno-format-security -lm `pkg-config --cflags --libs gtk+-3.0` -export-dynamic

